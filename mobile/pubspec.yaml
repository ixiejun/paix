name: mobile
description: A new Flutter project for AI Spot DEX.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Web3 Dependencies
  http: ^1.1.0
  convert: ^3.1.1
  web3dart: ^3.0.1
  wallet: ^0.0.18
  # Secure Storage for Private Keys
  flutter_secure_storage: ^9.0.0
  # Substrate Dependencies
  polkadart: ^1.0.0
  polkadart_scale_codec: ^2.0.0
  substrate_metadata: ^2.0.0
  polkadart_keyring: ^0.7.2
  sr25519: ^0.7.2
  ss58: ^2.0.0
  bip39_mnemonic: ^4.0.1
  pointycastle: ^4.0.0
  local_auth: ^3.0.0
  # State Management
  provider: ^6.1.1
  # UI/UX
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.14 # For rendering AI responses

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
